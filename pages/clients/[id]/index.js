// this page will be visible when the url like this: http://localhost:3000/clients/id
// all these pages we can get access to the concrete values entered in the url by using useRouter hook

const ClientProjectsPage = () => {
  return (
    <div>
      <h1>The projects of given client</h1>
    </div>
  );
};
export default ClientProjectsPage;
